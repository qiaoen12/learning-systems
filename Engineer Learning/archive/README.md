# 历史快照说明

版本：1.1
整理日期：2026-08-26
最近修订：2026-08-26（3.3，见第 7 节）

本目录只做追溯，**不参与当前执行**。快照保持原文逐字不动，不在这里修订；需要纠错时，在 `../current/` 里写新判断。

默认会话不读本目录。只有三种情况才进来：比较版本、追溯原文措辞、审计这次迁移。

## 1. 已同步快照

| 文件 | 原始来源 | 角色 |
| --- | --- | --- |
| `corpus-first-v3.2-original.md` | `AI student/learning-plan-2026-09-corpus-first.md` | 3.2 原案的逐字源稿，510 行 |
| `v3.2-clean/`（4 个文件） | git 提交 `a703a7c` 的 `current/` | 被 3.3 替代的 3.2 清洁版，711 行 |
| `learning-plan-final-v2.0.md` | `AI student/learning-plan-final.md` | 被隔离的旧 16 周执行版，844 行 |
| `howto-v1.0.md` | `AI student/frozen/howto.md` | 更早的 16–24 周通用框架，365 行 |

前者的原始来源目录：`/Users/qiaoen/Projects/80-knowledge-data/80-research/AI student/`

`v3.2-clean/` 逐字取自 git，不是从工作树复制的——工作树在导出时已经改成 3.3 了。取出后与 `git show HEAD:...` 的 SHA-256 逐一比对通过。git 历史本身也是这份快照的第二份副本。

## 2. 校验值

以下 SHA-256 在复制后与原文件逐一比对通过，用于确认快照未被改写：

```text
0e11ee13e0e10735d69e41f29e9f509a4e7971aa8f671760bc0164434d48e175  corpus-first-v3.2-original.md
7790fd8b213b75cf60235eaf32eb5635e1492385639b2c15be2ef24f60aff2f8  learning-plan-final-v2.0.md
1df01ac90b83f0e4b55757de49171afe96a51f4110dd18ca7285b1d821e47e98  howto-v1.0.md
f547a341bba05e313c309c16689cf46548a09554f456109de4730598512230cf  v3.2-clean/00-overview.md
3199094ba76086967534f773e621289c0d20d477da4bc9d9147fc86f2b3fdcba  v3.2-clean/01-guardrails.md
9e8fb94facb26b83b181027c6634127a61063d8d14ef18b9a633891754dec13c  v3.2-clean/02-milestones.md
325b84d23eb87a9c4bf393c7784cc781b7c2591296009e0a1e7fd6c0e497b1be  v3.2-clean/03-execution.md
```

复核命令：

```bash
cd "/Users/qiaoen/Projects/80-knowledge-data/80-research/Engineer Learning/archive"
shasum -a 256 -c CHECKSUMS.txt
```

## 3. 三份快照的关系

```text
howto-v1.0            早期通用框架，16–24 周，四层能力模型
      ↓  被吸收
learning-plan-final   16 周单链路，假设根因是「知识不足」
      ↓  被替代（不是被否证：它只是回答了另一个问题）
corpus-first-v3.2     假设根因是「缺少无法自我欺骗的反馈回路」  ← 现行版来源
```

3.2 原案自身声明与 `learning-plan-final` **并行存在、不覆盖不替代**。本目录选择 3.2 作为现行版，是一次执行决策，不是对旧版的技术否证。旧版的六条反退化阀门仍被现行版继承，见 `../current/01-guardrails.md` 第 6 节。

## 4. 故意没有同步的材料

以下材料留在原 `AI student/` 目录，需要审计时定向读取，不复制进来。

| 材料 | 位置 | 为什么不复制 |
| --- | --- | --- |
| `QA.md`（1644 行） | `AI student/frozen/QA.md` | 能力盘点原始问答。当前所需事实已压缩进 `../context/learner-profile.md`；全文会挤爆单会话上下文 |
| `learning-plan-codex.md` | `AI student/frozen/` | 不同 AI 生成的对照方案，已被 final 2.0 吸收 |
| `learning-plan-opencode.md` | `AI student/frozen/` | 同上 |
| `learning-plan-融合.md` | `AI student/frozen/` | 同上 |
| `deep-research-report.md` | `AI student/frozen/` | final 2.0 的升级依据，已体现在 final 2.0 里 |
| `learning-systems/`（15 文件，独立 git 仓库） | `AI student/learning-systems/` | 课程与资料索引。现行版明确禁止扩展这类清单 |
| CS2023 知识地图、roadmap.sh 双语方案、翻译批次 | `AI student/docs/` | 同上，属于入口阀门禁止新增的元文档类别 |
| 软件知识地图（md / html / png / pdf） | `AI student/` 根与 `output/` | 同上 |

**这些材料不是垃圾，但它们正是失败模式的证物**：8 周里它们产出了四五千行，而练习代码是 0 行。把它们排除在默认读取路径之外，是本次结构调整的目的之一，不是遗漏。

## 5. 清洁切分（结构 1.0）对 3.2 原案做的调整

结构 1.0 是 3.2 原案的重排。以下四处与原文不是逐字对应：

| 处 | 原案 | 3.2 清洁版 | 原因 | 性质 |
| --- | --- | --- | --- | --- |
| 重算判据 A 级 | 写「重现 6 个数字」，但只列出 5 项 | 补上 `source_file_count`，凑齐 6 项 | 原文内部计数不一致；manifest 里除 `canonical_sha256`（属 D 级）外恰好 6 个数字 | 非语义 |
| 入口阀门触发条件 | 「`corpus-guard` 连续 7 天没有 commit」 | 「产生第一个 commit 之后，连续 7 天没有新 commit」 | 原文在仓库尚不存在时无法判定触发 | **语义** |
| 门内阀门六条 | 只写「原有六条继续保留有效」，正文不列 | 六条逐条列入 `../current/01-guardrails.md` 第 6 节 | 消除对 `learning-plan-final-v2.0.md` 的运行时依赖，使 `current/` 自足 | 非语义 |
| 「第一天做什么」 | 无 | `../current/03-execution.md` 第 6 节新增 3 步 | 纯操作衔接，不引入新规则 | 非语义 |

**一处已更正的分类。** 本节 1.0 版把上面四处统称为「非语义调整」，`../README.md` 也据此声明「未改动任何规则语义」。这不成立：改变一条规则的触发时机就是语义改动。按 `../AGENTS.md` 的版本规则，它当时应该升版本号而没有升。更实际的后果是这次改动引入了冷启动漏洞——见第 7 节第 2 条。3.3 修订时更正了分类，并补上了缺失的那一态。

其余内容按章节重排：原案 §1–3 → `00-overview.md`；§5、5.1、5.2、6 → `01-guardrails.md`；§7 与九月验收 → `02-milestones.md`；§4、8、9、10、11 → `03-execution.md`。

## 6. 旧目录的状态

`AI student/` 的历史材料**未被修改、未被删除**，原样保留。

**唯一的例外是 `AI student/AGENTS.md`**，3.3 修订时给它加了归档墓碑。原因是这不是一处普通的过期措辞，而是活跃的指令污染：

- 它原本在第 3 行写「当前执行版路线为根目录的 `learning-plan-final.md`」，并在第 9、16、40 行重复这一断言。
- 代理进入 `AI student/` 工作时会加载这份 `AGENTS.md` 作为规则，而不会先去读 `../Engineer Learning/README.md` 的版本边界。
- 也就是说，1.0 版靠「文档优先级」解决冲突的办法，对只加载嵌套 `AGENTS.md` 的代理无效。

墓碑同时点名了旧目录根上的两份计划文件。`learning-plan-2026-09-corpus-first.md` 是现行版的源稿副本，看起来比 `learning-plan-final.md` 新，最容易被误当成当前版，但它不随 `../current/` 更新。

这条修改与 `../AGENTS.md` 写入边界的关系：该处已相应放开，允许对旧目录做归档标注，不允许改动其余内容。

## 7. 3.2 清洁版 → 3.3 的修订

修订日期：2026-08-26
起因：正式启动前的一次规则审查，逐条核对了文档声明与磁盘事实。
性质：**规则语义修改**。按 `../AGENTS.md` 的版本规则，这类修改必须升版本号、写修订说明、把被替代版本冻结进本目录。三件都已做。

被替代的版本快照在 `v3.2-clean/`，逐字取自 git，校验值见第 2 节。

### 7.1 改了什么

| # | 处 | 3.2 清洁版 | 3.3 | 为什么 |
| --- | --- | --- | --- | --- |
| 1 | 旧目录指令污染 | `AI student/AGENTS.md` 声明「当前执行版路线为 `learning-plan-final.md`」，靠 `../README.md` 的版本边界解决冲突 | 加归档墓碑，断言改成过去式；`../AGENTS.md` 的写入边界相应开一个归档标注的口子 | 代理进入旧目录会把那份 `AGENTS.md` 当规则加载，不会先读新目录的版本边界。文档优先级对它无效 |
| 2 | 入口阀门 | 「产生第一个 commit 之后，连续 7 天没有新 commit」 | 补冷启动态：无第一个 commit 时锁定，唯一允许的动作是 L0 | 永不产生第一个 commit 就永不触发，而这正是最该被检测的失败模式，也正是 2026-08-26 的实际状态 |
| 3 | 6 小时语法上限 | 既是「硬上限」，又是 L0.5 通过条件和九月出口的达标项，同时自认「这个数字是拍的」 | 对输入动作硬停，不作层级门禁 | 用一个自认无依据的数字当淘汰线，自相矛盾。约束落在输入动作上才有意义 |
| 4 | L0.5 的 ≥10 questions | 判据里写 ≥10 条，通过条件里没有；四个文件三种说法 | 统一降为诊断指标 | 条数可以刷，凑数恰好污染 `questions.md` 唯一的用处 |
| 5 | 宽松期 / 严格期 | 按日历切换：09-01 至 09-14 宽松，09-15 起严格 | 按能力切换：连续两轮主动删改 AI 建议 + 断电测试「读」级通过 | 与「每一层没有截止日」冲突；更实际的问题是 L1 拖到 9 月中旬才开始时，脚手架整段消失 |
| 6 | 分区定义 | `README.md` 说两个区（`archive/` 算合约区），`AGENTS.md` 是四行权限表 | 统一为四种角色 | 同一件事两种定义，长期会重新长出多个事实源 |
| 7 | 默认读取路径 | `README.md` 说 4 个文件，`AGENTS.md` 与 `records/README.md` 说 5 个 | 统一为 5 个 | 同上 |
| 8 | 碎片时间 | 每天 ×10 段，读 `corpus-guard` 里的一个函数 | 改成每天至多 10 段的机会制，并补上 L0 到 L1 第一步的替代读取对象 | ×10 写成硬性节奏等于把学习设计成新负担；而且 L0 全程和 L1 第一步 `corpus-guard` 里根本没有代码，这是个空指针 |
| 9 | L5 评估顺序 | 先写 30 条黄金样本 → 再写 judge → 最后生成，归因给 evaluation-driven development | 真实输出 → 错误分析 → 归纳分类 → 建样本与 judge | Hamel Husain 的公开立场是为发现的错误写评估器、不为想象的错误写；评估先行留给成功标准确定的场景，也就是 L1 那一类。3.2 把 L1 的做法搬到 L5，方向反了 |
| 10 | 语料备份的排期 | 整件事排在 L3，十月起按需 | 应急部分提前做掉，L3 只剩异地副本和 L1 把关的恢复 | 它不是学习任务，是资产安全前置条件。实测：`discord/` 无 `.git`、`reddit/` 与 `Router/` 各 0 commit、本机无 Time Machine 目标，约 590MB 语料单盘单副本。方案自己写着「语料备份风险最急」却排到十月，顺序不成立 |

第 10 条已执行：冷备在 `~/Backups/corpus-80-data/2026-08-26/`，29612 个文件的 SHA-256 清单，恢复演练 74/74 通过，恢复出的 canonical 与生产盘逐字节相同。**仍是同盘副本，异地副本待外置盘或云目标。**

### 7.2 故意没做的四项

这些是同一轮审查里认可但推后的。记在这里是为了它们不被遗忘，也为了说明推后不是遗漏。

| 项 | 内容 | 什么时候做 |
| --- | --- | --- |
| 能力证据阀门 | 入口阀门现在只检测有没有 commit，而 commit 本身也可以刷。真正该检测的是有没有新的能力证据：新增一个真实测试、多覆盖一个真实实例、完成一次失败样本的判断、断电测试升级、canonical 重算达到更高等级 | 运行 2 到 4 周、拿到真实节奏数据之后。它叠加在机械检测之上，不替换——任何需要自评的指标对已经停摆的人都失效 |
| L1 的极小独立写 | 现在第一次「从空文件写」的正式要求在 L3（断电测试「写」级）。改法不是新增步骤，而是把 `../current/01-guardrails.md` 第 2 节升级阶梯最后一档的「补一条 pytest」明确成「自己从空文件写」 | L1 出现第一次真实卡点之后。提前写会变成预设，而这一层的原则是由失败拉动 |
| adapter 边界 | 一条中文规则对一个 pytest 是好脚手架，但不是独立验证：规则理解错了、AI 忠实实现了错的规则，测试照样全绿。需要一层固定的测试接口，让实现可以被重写而标准答案不跟着变。参照 Stanford CS336 作业的 adapter 层 | L2 开工时。现在做有两个问题：它是一层抽象，跟 `../current/01-guardrails.md` 第 3 节「禁抽象、重复优先」冲突，除非写成一组平凡函数；而且 L1 阶段 manifest 本身就是外部标准答案，问题还不严重 |
| 风格契约的解除阶梯 | 契约写清了什么时候装辅助轮，没写什么时候拆。只有类型注解有隐式解锁（「L2 之前一律不要」），其余八条没有。长期只读教学版 Python，会读不懂现实中正常的 Python | L3。不新造阶段划分，直接挂到 L3 已有的目标上：每在旧代码里读懂一个被禁构造的实际用法，就解除它在 `corpus-guard` 里的禁令，并用它重构一个已经理解的函数 |

### 7.3 一条自我约束

3.3 定稿即进入冷启动锁定（`../current/01-guardrails.md` 第 5.1 节）。`corpus-guard` 出现第一个 commit 之前，不再修改 `../current/`。

第 7.2 节的四项，以及运行中冒出的任何新想法，都不当场改，先记进 `../records/questions.md` 或 `corpus-guard/docs/DECISION_LOG.md`。判断这次重构成功与否的标准是：两三周后 `corpus-guard` 里出现了测试、报告、失败样本和断电测试记录，而 `../current/` 基本没变。
